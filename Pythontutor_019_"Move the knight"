x1 = int(input())
y1 = int(input())
x2 = int(input())
y2 = int(input())
if (abs(x1 - x2) == 2 and abs(y1 - y2) == 1) or (abs(x1 - x2) == 1 and abs(y1 - y2) == 2):
    print ("YES")
else:
    print ("NO")
    
 '''
 The task "Move the knight" the condition of the Chess knight moves by 
 the letter "l" — two squares vertically in any direction and one square horizontally, or Vice versa. 
 The two different squares of the chessboard, will determine whether the horse to get from the first cell to the second one. 
 In all tasks read input using input() and print the answer using print().
    
   Задача «Ход коня»
Условие
Шахматный конь ходит буквой “Г” — на две клетки по вертикали в любом направлении и на одну клетку по горизонтали, или наоборот. 
Даны две различные клетки шахматной доски, определите, может ли конь попасть с первой клетки на вторую одним ходом.

Во всех задачах считывайте входные данные через input() и выводите ответ через print().'''
